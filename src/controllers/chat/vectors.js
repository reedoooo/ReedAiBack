// const express = require('express');
// const { asyncHandler } = require('../../utils/api/sync.js');
// const *: vectorController = require('../controllers/vectorController.js');
// const { authenticate } = require('../middleware/auth.js');

// const router = express.Router();

// router.use(authenticate);

// router.post('/upsert-docs', asyncHandler(vectorController.upsertDocs));
// router.post('/query-components', asyncHandler(vectorController.queryComponents));

// export default router;
